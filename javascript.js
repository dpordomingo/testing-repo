// Set variavles
var user = []; //inline coment

/**
 * Adds two numbers together
 * 
 * @param {number} first The First Number
 * @returns {number}
 */
function add(first) {
  /*
    block coment
    gos ere
  */
  if (a != true) {
    return i;
  }
  
  return first++;
}

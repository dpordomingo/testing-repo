// set variavles
var user = []; //inline coment

/**
 * adds two numbers togeter
 * 
 * @param {number} first the first number
 * @returns {number}
 */
function add(first) {
  /*
    block coment
    goes ere
  */
  if (a != true) {
    return i;
  }
  
  return first++;
}
